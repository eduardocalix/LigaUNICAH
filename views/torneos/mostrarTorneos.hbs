<div class="container">
  <br><br><br><br><br>
    {{#if user}} 
        <div class="row">
            {{#each torneos}}
                <div class="col-md-6">
                    <div class="card">
                        <div class="card-body">
                            <h3 class="card-title d-flex justify-content-between align-items-center">
                                Nombre del Torneo: {{ nombreTorneo }}
                                <br>
                                Categoria: {{categoria}} <a href=""><i class="fas fa-edit"></i></a>
                            </h3>
                            <h3 class="card-title d-flex justify-content-between align-items-center">
                                Periodo: {{periodo}}
                            </h3>
                            <div class="card-title d-flex justify-content-between align-items-center"> 
                                <span>
                                    <p>{{year}}</p>
                                </span>
                            </div>
                            <form action="/delete/{{_id}}" method="POST">
                            {{!-- <input type="hidden" name="_method" value="DELETE"> --}}
                                <a href="/nuevoEquipo/{{url}}" class="btn btn-success btn-block">Crear nuevo Equipo</a>
                                <br><br>
                                <button type="submit" class="btn btn-danger btn-block btn-sm">Eliminar este torneo </button>
                                <br>
                                <p>Equipos de este torneo</p>
                                <a href="" class="btn btn-success btn-block">Ver los equipos</a>
                                <br><br>
                            </form>
                        </div>
                    </div>
                </div>
            {{else}}
                <div class="card mx-auto">
                    <div class="card-body">
                        <h1>Hola {{nombre}}</h1>
                        <p clsss="lead">Todavía no tienes torneos creados!</p>
                        <a href="/nuevoTorneo" class="btn btn-success btn-block">Crear un Torneo</a>
                    </div>
                </div>
            {{/each}}
        </div>
</div>